class LabelService {
  // Todo 1. Get all
  async pub() {
    const data = {
      id: "id",
      name: "name",
    };
    return data;
  }

  // Todo 2. Get detail
}

module.exports = new LabelService();
